import { HeaderColumnNamesPipe } from './header-column-names.pipe';

describe('HeaderColumnNamesPipe', () => {
  it('create an instance', () => {
    const pipe = new HeaderColumnNamesPipe();
    expect(pipe).toBeTruthy();
  });
});
